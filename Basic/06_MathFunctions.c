#include <stdio.h>
#include <math.h>
int main()
{
    int x = 9;

    // x = sqrt(x);
    // x = pow(x,2);
    // x = round(x);
    // x = ciel(x);
    // x = floor(x);
    // x = abs(x);
    // x = log(x);
    // x = sin(x);
    // x = cos(x);
    // x = tan(x);

    printf("%d", x);

    return 0;
}