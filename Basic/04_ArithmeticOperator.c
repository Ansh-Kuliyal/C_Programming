#include <stdio.h>
int main()
{
    // arithmetic operator = + - * / % ++ --

    int x = 2;
    int y = 3;
    int z;
    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;
    // z = x % y;

     //x+=3;
     //x-=3;
     //x*=2;
     //x/=2;
    printf("%d", x);
    return 0;
}